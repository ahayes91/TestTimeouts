import keyMirror from '@test/react-utils/src/keyMirror';

export const BOG_ACTIONS = keyMirror({
  FETCH_BOG_DETAILS: null,
  LOAD_BOG_DETAILS: null,
});

export const APP_ACTIONS = keyMirror({
  CLEAR_REDUX_STORE: null,
});

export default BOG_ACTIONS;
